{
  "name": "password_generator",
  "version": "1.0.0",
  "description": "App to generate password",
  "main": "main.js",
  "scripts": {
    "start": "concurrently \"npx electron .\" \"flask --app ./src/backend/api.py run\" -k",
    "install-deps": "npm install && pip install Flask Flask-Cors"
  },
  "author": "Doy",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "electron": "^24.3.0"
  },
  "dependencies": {
    "ps-node": "^0.1.6",
    "tree-kill": "^1.2.2"
  }
}
